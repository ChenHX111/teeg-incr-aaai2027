"""Cycle 10 edit operators (Day 2-3 deliverable).

One operator per method: applies an EditSet to a baseline bundle and returns
an edited bundle ready for the corresponding query algorithm to run on.

This package exposes the timing-instrumented edit operators so the timing
harness (Day 4) can measure graph-edit-wall-time per (method, scenario, intensity).
"""
